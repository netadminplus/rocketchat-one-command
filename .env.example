# RocketChat Configuration Example
# The installer generates docker-compose.yml directly, so this file is just for reference

# Example values:
DOMAIN=chat.example.com
PORT=3000
RELEASE=latest
ROOT_URL=https://chat.example.com
